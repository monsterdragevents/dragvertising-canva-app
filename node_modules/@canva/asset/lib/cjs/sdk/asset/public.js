"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: all[name]
    });
}
_export(exports, {
    upload: function() {
        return upload;
    },
    getTemporaryUrl: function() {
        return getTemporaryUrl;
    },
    findFonts: function() {
        return findFonts;
    },
    requestFontSelection: function() {
        return requestFontSelection;
    },
    openColorSelector: function() {
        return openColorSelector;
    }
});
const { canva } = window;
function upload(options) {
    return canva.content.upload(options);
}
function getTemporaryUrl(options) {
    return canva.content.getTemporaryUrl(options);
}
function findFonts(options) {
    return canva.content.findFonts(options);
}
function requestFontSelection(request = {}) {
    return canva.content.requestFontSelection(request);
}
function openColorSelector(anchor, options) {
    return canva.content.openColorSelector(anchor, options);
}
/**
 * @internal
 * When it comes to method overloads, API extractor is omitting the last signature, even if it has
 * the public identifier, that's why the generic upload the returns `QueuedAsset` is repeated
 */ // eslint-disable-next-line @typescript-eslint/no-unused-expressions
'comment';
