import type { Argument as ClassNameArgument } from 'classnames';
import * as React from 'react';
export type BaseScrollableProps = {
    children?: React.ReactNode
    direction: ScrollDirection
    onScroll?: (getScrollState: () => ScrollState) => void
    ariaLabel?: string
    role?: 'region' | 'group' | 'list'
    className?: ClassNameArgument;
};
export type ScrollableRef = {
    getScrollState: () => ScrollState
    scrollTo: (options: ScrollToOptions) => void
    scrollBy: (options: ScrollByOptions) => void
};
export type ScrollState = {
    scrollable: false
} | {
    scrollable: true
    atStart: boolean
    atEnd: boolean
    raw: {
        scroll: {
            height: number
            width: number
            left: number
            top: number
        }
        client: {
            height: number
            width: number
            left: number
            top: number
        }
    }
};
export type ScrollDirection = 'vertical' | 'horizontal';
export type ScrollByOptions = {
    top?: number;
    left?: number;
    behavior?: 'auto' | 'smooth';
};
export type ScrollToOptions = ScrollByOptions & {
    position?: 'start' | 'end';
};
export declare const BaseScrollable: React.ForwardRefExoticComponent<BaseScrollableProps & React.RefAttributes<ScrollableRef>>;
