"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get TruncatedList () {
        return TruncatedList;
    },
    get useTruncatedList () {
        return useTruncatedList;
    }
});
const _jsxruntime = require("react/jsx-runtime");
const _react = _interop_require_wildcard(require("react"));
const _base_selectcss = _interop_require_default(require("./base_select.css"));
const _base_selectmessages = require("./base_select.messages");
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
function useTruncatedList({ items, joiner = ', ', renderOverflowText = (overflow)=>_base_selectmessages.BaseSelectMessages.hiddenOptions(overflow) }) {
    const containerRef = _react.useRef(null);
    const itemsTextRef = _react.useRef(null);
    const overflowTextRef = _react.useRef(null);
    const [itemsText, setItemsText] = _react.useState('');
    const [overflowText, setOverflowText] = _react.useState('');
    _react.useLayoutEffect(()=>{
        const containerEl = containerRef.current;
        const itemsTextEl = itemsTextRef.current;
        const overflowTextEl = overflowTextRef.current;
        if (containerEl == null || itemsTextEl == null || overflowTextEl == null) return;
        const overflow = items.length - 1;
        let overflowText = overflow ? renderOverflowText(overflow) : '';
        let itemsText = `${items[0]}${overflow > 0 ? joiner : ''}`;
        const maxWidth = containerEl.offsetWidth;
        for (let i = 1; i < items.length; i++) {
            const testRenderedCount = i + 1;
            const testOverflow = items.length - testRenderedCount;
            const testOverflowText = testOverflow ? renderOverflowText(testOverflow) : '';
            const testItemsText = `${items.slice(0, testRenderedCount).join(joiner)}${testOverflow > 0 ? joiner : ''}`;
            const currentOverflowText = overflowTextEl.textContent;
            const currentItemsText = itemsTextEl.textContent;
            overflowTextEl.textContent = testOverflowText;
            itemsTextEl.textContent = testItemsText;
            const textWidth = itemsTextEl.offsetWidth + overflowTextEl.offsetWidth;
            if (textWidth > maxWidth) {
                overflowTextEl.textContent = currentOverflowText;
                itemsTextEl.textContent = currentItemsText;
                break;
            }
            itemsText = testItemsText;
            overflowText = testOverflowText;
        }
        setItemsText(itemsText);
        setOverflowText(overflowText);
    }, [
        items,
        joiner,
        renderOverflowText
    ]);
    return {
        containerRef,
        itemsTextRef,
        overflowTextRef,
        itemsText,
        overflowText
    };
}
const TruncatedList = _react.memo(({ items, joiner, renderOverflowText, tagName: TagName = 'span' })=>{
    const { containerRef, itemsTextRef, overflowTextRef, itemsText, overflowText } = useTruncatedList({
        items,
        joiner,
        renderOverflowText
    });
    return (0, _jsxruntime.jsxs)(TagName, {
        ref: containerRef,
        className: _base_selectcss.default.truncatedList,
        children: [
            (0, _jsxruntime.jsx)("span", {
                className: _base_selectcss.default.renderedText,
                children: (0, _jsxruntime.jsx)("span", {
                    ref: itemsTextRef,
                    children: itemsText
                })
            }),
            (0, _jsxruntime.jsx)("span", {
                className: _base_selectcss.default.remainingText,
                children: (0, _jsxruntime.jsx)("span", {
                    ref: overflowTextRef,
                    children: overflowText
                })
            })
        ]
    });
}, (prevProps, nextProps)=>{
    return prevProps.items.length === nextProps.items.length && prevProps.items.every((item, index)=>item === nextProps.items[index]);
});
