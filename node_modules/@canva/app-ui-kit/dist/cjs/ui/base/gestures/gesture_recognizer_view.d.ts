import * as React from 'react';
import type { GestureElement, Recognizer } from './recognizer';
export type GestureRef = (ref: GestureElement | null) => void;
export type GestureRecognizerProps = {
    recognizers: readonly Recognizer[]
    children(props: {
        gestureRef: GestureRef;
    }): React.ReactNode;
    raf?: Window['requestAnimationFrame'];
    caf?: Window['cancelAnimationFrame'];
};
export declare class GestureRecognizerView extends React.PureComponent<GestureRecognizerProps> {
    private readonly inner;
    render(): React.ReactNode;
    private readonly gestureRef;
    componentDidUpdate(prevProps: Readonly<GestureRecognizerProps>): void;
}
