"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get accessModeState () {
        return accessModeState;
    },
    get useIsMouseAccessMode () {
        return useIsMouseAccessMode;
    }
});
const _make_observable = require('../../../../base/make_observable/make_observable');
const _mobx = require("mobx");
const _react = _interop_require_wildcard(require("react"));
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
const initialAccessMode = 'mouse';
class AccessModeStore {
    static _makeObservable(instance) {
        (0, _make_observable.makeObservable)(instance, {
            _accessMode: _mobx.observable.ref,
            isMouseMode: _mobx.computed,
            isKeyboardMode: _mobx.computed,
            accessMode: _mobx.computed,
            setAccessMode: _mobx.action
        });
    }
    get isMouseMode() {
        return this._accessMode === 'mouse';
    }
    get isKeyboardMode() {
        return this._accessMode === 'keyboard';
    }
    get accessMode() {
        return this._accessMode;
    }
    setAccessMode(mode) {
        this._accessMode = mode;
        return this._accessMode;
    }
    resetAccessMode() {
        this.setAccessMode(initialAccessMode);
    }
    constructor(){
        this._accessMode = (AccessModeStore._makeObservable(this), initialAccessMode);
    }
}
const accessModeState = new AccessModeStore();
function registerDocumentListeners(document1) {
    document1.addEventListener('keydown', (e)=>{
        if (e.key === 'Tab') accessModeState.setAccessMode('keyboard');
    }, true);
    document1.addEventListener('mousedown', (e)=>accessModeState.setAccessMode('mouse'), true);
    document1.addEventListener('touchstart', (e)=>accessModeState.setAccessMode('mouse'), true);
}
if (typeof document !== 'undefined') registerDocumentListeners(document);
function useReaction(expression, initialValue) {
    const [state, setState] = _react.useState(initialValue);
    _react.useEffect(()=>{
        return (0, _mobx.reaction)(expression, (value)=>setState(value), {
            fireImmediately: true
        });
    }, []);
    return state;
}
function useIsMouseAccessMode() {
    return useReaction(()=>accessModeState.isMouseMode, true);
}
