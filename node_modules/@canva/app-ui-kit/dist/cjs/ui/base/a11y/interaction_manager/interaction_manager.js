"use strict"
Object.defineProperty(exports, "__esModule", {
    value: true
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get InteractionManager () {
        return InteractionManager;
    },
    get useInteractionLayer () {
        return useInteractionLayer;
    }
});
const _jsxruntime = require("react/jsx-runtime");
const _react = _interop_require_wildcard(require("react"));
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
const InteractionContext = _react.createContext(undefined);
function useInteractionLayer({ markOutsideInert }) {
    const context = _react.useContext(InteractionContext);
    const element = _react.useRef(null);
    const ref = _react.useCallback((newElement)=>{
        const oldElement = element.current;
        element.current = newElement;
        if (newElement !== oldElement && oldElement != null)
            context === null || context === void 0 ? void 0 : context.removeLayer(oldElement);
        if (newElement != null) context === null || context === void 0 ? void 0 : context.registerLayer({
            element: newElement,
            markOutsideInert
        });
    }, [
        context,
        markOutsideInert
    ]);
    _react.useEffect(()=>{
        if (element.current != null) context === null || context === void 0 ? void 0 : context.registerLayer({
            element: element.current,
            markOutsideInert
        });
        return ()=>{
            if (element.current != null) context === null || context === void 0 ? void 0 : context.removeLayer(element.current);
        };
    }, [
        context,
        markOutsideInert
    ]);
    return ref;
}
function InteractionManager({ children }) {
    const [layers, setLayers] = _react.useState([]);
    const registerLayer = _react.useCallback((layer)=>{
        setLayers((prevLayers)=>{
            let existingLayer = false;
            const newLayers = [];
            prevLayers.forEach((l)=>{
                if (l.element === layer.element) {
                    existingLayer = true;
                    newLayers.push(layer);
                } else newLayers.push(l);
            });
            if (!existingLayer) newLayers.push(layer);
            return newLayers;
        });
    }, [
        setLayers
    ]);
    const removeLayer = _react.useCallback((element)=>{
        setLayers((prevLayers)=>prevLayers.filter((layer)=>layer.element !== element));
    }, [
        setLayers
    ]);
    _react.useEffect(()=>{
        let belowInertLayer = false;
        [
            ...layers
        ].reverse().forEach((layer)=>{
            var _layer_element;
            (_layer_element = layer.element) === null || _layer_element === void 0 ? void 0 : _layer_element.toggleAttribute('inert', belowInertLayer);
            if (layer.markOutsideInert)
                belowInertLayer = true;
        });
    }, [
        layers
    ]);
    const value = _react.useMemo(()=>({
            registerLayer,
            removeLayer
        }), [
        registerLayer,
        removeLayer
    ]);
    return (0, _jsxruntime.jsx)(InteractionContext.Provider, {
        value: value,
        children: children
    });
}
