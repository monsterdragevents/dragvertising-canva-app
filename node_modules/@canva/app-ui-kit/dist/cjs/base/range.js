"use strict"
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "range", {
    enumerable: true,
    get: function() {
        return range;
    }
});
function range(startOrLength, endExclusive) {
    const start = endExclusive != null ? startOrLength : 0;
    const length = endExclusive != null ? Math.ceil(endExclusive - start) : Math.floor(startOrLength);
    return new Array(Math.max(0, length)).fill(0).map((_, i)=>start + i);
}
