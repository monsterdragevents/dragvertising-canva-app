import { jsx as _jsx } from "react/jsx-runtime";
import { UnreachableError } from '../../../../../../../../base/preconditions';
import { InputPill as EaselInputPill } from '../../../../../../../base/form/pills_input/pills_input';
/** 
 * Pills to represent the items entered in an input.
 */ export function InputPill({ truncateText, ...props }) {
    return _jsx(EaselInputPill, {
        text: props.text,
        tone: props.tone,
        disabled: props.disabled,
        maxWidth: mapMaxWidth(truncateText),
        onRemoveClick: props.onRemoveClick
    });
}
function mapMaxWidth(truncateText) {
    switch(truncateText){
        case undefined:
            return undefined;
        case true:
            return '25u';
        case false:
            return '100%';
        default:
            throw new UnreachableError(truncateText);
    }
}
