import { jsx as _jsx } from "react/jsx-runtime";
import classNames from 'classnames';
import * as React from 'react';
import { Placeholder } from '../../../placeholder/placeholder';
import styles, { getStyle } from './placeholder.css';
export const TextPlaceholder = React.memo((props)=>_jsx(TextPlaceholderComponent, {
        ...props
    }));
export const TitlePlaceholder = React.memo((props)=>_jsx(TextPlaceholderComponent, {
        ...props,
        variant: "title"
    }));
function TextPlaceholderComponent({ size = 'medium', variant = 'regular', index, disableAnimations }) {
    const containerClassName = classNames(getStyle(size), {
        [styles.title]: variant === 'title'
    });
    return _jsx("div", {
        className: containerClassName,
        children: _jsx(Placeholder, {
            shape: "textRectangle",
            index: index,
            disableAnimations: disableAnimations
        })
    });
}
