import { jsx as _jsx } from "react/jsx-runtime";
import * as React from 'react';
import { BaseScrollable } from './base_scrollable';
import { ScrollableWithIndicator } from './scrollable_with_indicator';
export const Scrollable = React.forwardRef((props, forwardedRef)=>{
    const { children, direction = 'vertical', onScroll, indicator, ariaLabel, role } = props;
    if (indicator) return _jsx(ScrollableWithIndicator, {
        direction: direction,
        onScroll: onScroll,
        ariaLabel: ariaLabel,
        role: role,
        ref: forwardedRef,
        type: indicator.type,
        background: indicator.background ? indicator.background : 'surface',
        children: children
    });
    return _jsx(BaseScrollable, {
        direction: direction,
        onScroll: onScroll,
        ariaLabel: ariaLabel,
        role: role,
        ref: forwardedRef,
        children: children
    });
});
