import { jsx as _jsx } from "react/jsx-runtime";
import { observer } from 'mobx-react-lite';
import 'react';
import { pointerInputState } from '../../../a11y/pointer_input_state/pointer_input_state';
import { BadgeGroup } from '../../../badge/badge';
import styles from '../card.css';
export const CardDecorator = observer(function CardDecorator({ location, stretch, children, transition = 'fade', isInteractive = false, ...props }) {
    var _props_visibility;
    const visibility = !pointerInputState.isMouseInput ? 'always' : (_props_visibility = props.visibility) !== null && _props_visibility !== void 0 ? _props_visibility : 'on-hover';
    return _jsx(BadgeGroup, {
        stretch: stretch,
        transition: transition,
        visibility: visibility,
        location: location,
        className: isInteractive ? styles.cardAction : undefined,
        children: children
    });
});
