import { createHTMLElementHandle } from './create_handle';
export function createLayoutHandle(ref) {
    return createHTMLElementHandle(ref, 'getBoundingClientRect', 'offsetHeight', 'offsetLeft', 'offsetTop', 'offsetWidth');
}
