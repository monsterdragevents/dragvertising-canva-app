import { jsx as _jsx } from "react/jsx-runtime";
import classNames from 'classnames';
import 'react';
import { TabsContext, useTabs } from './behavior';
import styles, { customProperties } from './tabs.css';
export const TabsProvider = ({ children, ...props })=>{
    const values = useTabs(props);
    return _jsx(TabsContext.Provider, {
        value: values,
        children: children
    });
};
export const Tabs = ({ children, height = 'auto', ...props })=>{
    return _jsx(TabsProvider, {
        ...props,
        children: _jsx("div", {
            className: classNames(styles.tabs, height !== 'auto' && styles.fixedHeight),
            style: {
                [customProperties.tabsHeight]: typeof height === 'number' ? `${height}px` : undefined
            },
            children: children
        })
    });
};
