import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import classNames from 'classnames';
import * as React from 'react';
import { Badge } from '../../../badge/badge';
import { Button } from '../../../button/button';
import styles from './button_decorator.css';
export const ButtonDecorator = React.memo(
    function ButtonDecorator({ label, icon, badge, onClick, active, disclosure, disabled, className }) {
        const button = _jsx(Button, {
            size: "small",
            variant: "tertiary",
            iconSize: "medium",
            icon: icon,
            onClick: onClick,
            tooltipLabel: label,
            tooltipPlacement: "top",
            ariaLabel: label,
            active: active,
            disclosure: disclosure,
            disabled: disabled,
            className: classNames(badge != null && styles.enablePointerEvents, className)
        });
        if (badge != null) return _jsxs("div", {
            className: classNames(styles.badgeContainer, styles.disablePointerEvents),
            children: [
                button,
                _jsx("div", {
                    className: styles.badge,
                    children: typeof badge === 'string' ? _jsx(Badge, {
                        tone: "assist",
                        shape: "circle",
                        text: badge,
                        ariaLabel: badge
                    }) : badge
                })
            ]
        });
        return button;
    }
);
