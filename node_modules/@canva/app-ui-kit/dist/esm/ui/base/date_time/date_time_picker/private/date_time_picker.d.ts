import * as React from 'react';
import type { TimeZone } from '../../utils/utils';
import type { DateTimePickerStore } from './date_time_picker_presenter';
import { DateTimePickerPresenter } from './date_time_picker_presenter';
export type DateTimePickerMode = 'datetime' | 'date';
export type DateTimePickerProps = {
    locale: string
    date?: string
    timezone?: TimeZone
    today?: string
    focusToday?: boolean
    showTodayButton?: boolean
    canSelectPastDates?: boolean
    maxSelectableDate?: string
    minSelectableDate?: string | 'today'
    timeInvalid?: boolean
    selectedDayInvalid?: boolean
    animate: boolean
    clearable?: boolean
    presenter?: DateTimePickerPresenter
    store?: DateTimePickerStore
    onChange: (date?: string) => void
    mode?: DateTimePickerMode
};
declare class DateTimePickerComponent extends React.Component<DateTimePickerProps> {
    private readonly presenter;
    private readonly store;
    private readonly keyMap;
    private _date?;
    static readonly defaultProps: Partial<DateTimePickerProps>;
    constructor(props: DateTimePickerProps);
    private get date();
    private get time();
    private get displayedDate();
    private get month();
    private get year();
    private get navigationDisabled();
    private readonly onChangeMonth;
    private readonly onSelectDay;
    private readonly selectToday;
    private readonly clearSelectedDay;
    private readonly onChangeTime;
    private moveFocusBy;
    private moveFocusToStartOfWeek;
    private moveFocusToEndOfWeek;
    private getKeyMap;
    private readonly Footer;
    render(): import("react/jsx-runtime").JSX.Element;
}
export declare class DateTimePicker extends DateTimePickerComponent {
}
export {};
