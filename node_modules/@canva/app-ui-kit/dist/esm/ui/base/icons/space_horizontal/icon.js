import { Internal } from '../internal/base_icon';
import spaceHorizontalSvg from './icon-distribute-horizontally.inline.svg';
const { create } = Internal;
export const SpaceHorizontalIcon = create({
    medium: spaceHorizontalSvg
});
