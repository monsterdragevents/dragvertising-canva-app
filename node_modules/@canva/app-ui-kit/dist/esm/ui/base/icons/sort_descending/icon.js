import { Internal } from '../internal/base_icon';
import sortSvg from './icon-sort-descending.inline.svg';
const { create } = Internal;
export const SortDescendingIcon = create({
    medium: sortSvg
});
