import { Internal } from '../internal/base_icon';
import flipHorizontalSvg from './icon-flip-horizontal.inline.svg';
const { create } = Internal;
export const FlipHorizontalIcon = create({
    medium: flipHorizontalSvg
});
