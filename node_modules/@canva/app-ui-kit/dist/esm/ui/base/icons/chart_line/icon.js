import { Internal } from '../internal/base_icon';
import chartLineSvg from './icon-chart-line.inline.svg';
const { create } = Internal;
export const ChartLineIcon = create({
    medium: chartLineSvg
});
