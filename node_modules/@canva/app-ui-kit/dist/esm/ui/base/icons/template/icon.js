import { Internal } from '../internal/base_icon';
import templateSvg from './icon-template.inline.svg';
const { create } = Internal;
export const TemplateIcon = create({
    medium: templateSvg
});
export const TemplatesTabIcon = create({
    medium: templateSvg
});
